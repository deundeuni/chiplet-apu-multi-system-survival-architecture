# ARCHITECTURE_STRATEGY.md — 단일형 vs 조립형 생존 아키텍처 정리 (v2.8.1 Final)

> 본 문서는 특정 기업이나 특정 브랜드를 배제하고, 시스템 생존성과 경제성을 동시에 확보하기 위한 **물리적 조립 방식(Modular Architecture)**의 구조적 차이와 표준화 논리를 다룹니다.  
> 본 문서는 CC BY 4.0으로 공개되며, 방어적 공개(Defensive Publication)를 목적으로 합니다.

---

## 1. 두 가지 반도체 조립 방식 비교

* **단일형 (Single-Die Integration)**
  * **구조**: 하나의 거대한 다이(Die)에 모든 연산 및 제어 블록을 통합하여 제조하는 방식입니다.
  * **특징**: 초기 제조 단가가 비교적 저렴하며, 블록 간 내부 배선 구조가 단순합니다.
  * **제약**: 특정 연산 영역에 단일 결함이 발생할 경우 전체 시스템을 폐기해야 하며, 영역별 전원 및 보안 격리에 한계가 존재합니다.

* **조립형 (Chiplet / Modular Integration)**
  * **구조**: 기능별로 분리된 개별 칩렛(블록)들을 패브릭 Interconnect로 상호 연결하는 방식입니다.
  * **특징**: 특정 블록에 장애가 발생해도 해당 영역만 격리하고 예비 블록으로 바통을 넘기는 자가치유가 가능합니다. 전원, 클럭, 보안 영역을 블록 단위로 독립화할 수 있습니다.
  * **제약**: 블록 간을 연결하는 브리지(TL Bridge) 인터페이스와 복잡한 물리·소프트웨어 제어 설계가 요구됩니다.

---

## 2. 이중 트랙 운용의 목적

두 방식은 우열의 문제가 아닌, **운용 목적과 적용 환경의 차이**에 따라 병행됩니다.

* **보급 및 비용 검증 트랙 (단일형)**: 단가 절감 및 일괄 제조 수율 검증을 위해 활용됩니다.
* **생존 및 보안 현장 트랙 (조립형)**: 데이터 센터, 자율주행, 산업 라인 등 무중단과 데이터 격리가 필수적인 환경에 적용됩니다. 외부 검증되지 않은 블록의 무단 접근을 차단하고, 자체 블록을 독립 격리하여 안전성을 확보합니다.

---

## 3. 조립형 아키텍처의 핵심 상호호환 논리

* **고성능 구성**: 핵심 제어 본체 블록 + 고성능 외부 연산 가속 블록 (TL Bridge 연결) → 최고 성능 확보
* **범용·우회 구성**: 외부 가속 블록 이탈/장애 시 내장 백업 연산 블록으로 연산 바통 이행 → 시스템 무중단 유지 및 원가 최적화

**핵심은 TL Bridge와 제어 OS 레이어의 표준화입니다.** TL Bridge가 전원/클럭을 독립 분리하고, 제어 레이어가 블록 상태를 투표로 검증하므로, 메인보드 및 소프트웨어 구조 수정 없이 요구사항에 따라 연산 블록만 선택적으로 착탈 및 교체할 수 있습니다.

---

## 4. 내부 기술 노하우의 점진적 축적

* **1단계**: 외부 고성능 블록을 연결하여 초기 시스템 성능 역량을 확보합니다.
* **2단계**: 보급형 및 현장용 실증 환경에서 자체 내장 블록을 병행 운용함으로써 자가치유 및 제어 노하우를 축적합니다.
* **3단계**: 외부 블록이 완전히 배제된 극단적 장애 상황에서도 시스템이 최소 기능(Failsafe)을 유지하며 작동하는 구조를 완성합니다.

---

## 5. 현재 관측되는 아키텍처 수렴 현상

시장에서는 외부 고성능 연산 블록과의 협력을 확대함과 동시에, 자체 연산 및 제어 블록으로의 전환을 병행하는 흐름이 관측됩니다. 이는 단일 구조에 의존하지 않고 **단일형과 조립형을 동시에 교차 검증하는 '이중 조립 전략'**의 단계적 증거입니다.

---

## 6. 생존형 조립 표준의 진화 (soma-moa 로드맵 v2.8.1)

* **v2.6–v2.7 규격 이력**: L0 독립 PMIC/클럭 분리, L1 물리 격리 및 열·전압(T-Reg) 억제, L0-S 보안 격리, $N \ge 3$ 가변 확장형 물리/가상/하이브리드 $N$중 투표, SW/AI 동적 연결 구조 확립
* **v2.8 구현 계층 무관성 및 제어 방식 포괄 (선행기술 범위 확장 확정)**:
  투표 및 격리 제어의 실행 계층은 특정 하드웨어 위치에 한정되지 않습니다. 아래 모든 구현 계층에서의 실행을 동일한 논리적 $N$중 투표 및 자가치유 절차로 포괄 정의합니다:
  * **하드웨어 계층**: 마이크로코드, 펌웨어(FW), 메모리 컨트롤러, IOMMU/MMU, TL Bridge 로직
  * **시스템 소프트웨어 계층**: OS 커널, 커널 드라이버, 인터럽트 핸들러, 하이퍼바이저, 스케줄러
  * **메모리/자원 제어 계층**: 메모리 격리 및 투표, 페이지 테이블 기반 격리, DMA 버퍼 검증, 레지스터 락
  * **상위 소프트웨어/AI 계층**: SW 에이전트, AI 가속기, AI 에이전트 간 동적 신뢰도 투표 오케스트레이션
  * **통합 포괄 원칙**: 하드웨어 마이크로코드, 커널 드라이버, 메모리 컨트롤러 등 구현 스택의 위치와 무관하게, $N$개의 논리적 주체(물리/가상/하이브리드)가 신뢰도 투표를 거쳐 결함 격리 및 자가치유를 수행하는 구조는 모두 본 규격의 선행기술 범위에 포함됩니다.
* **통합 구현 실효 예시**:
  * 물리 3개 블록 + 커널 드라이버 가상 2개 노드 결합 = $N=5$ (하드웨어 물리 결합 + 가상화 노드 조합)
  * 메모리 컨트롤러 레벨 $5$중 페이지 투표 수행 및 1개 영역 오염 시 해당 페이지 물리 격리
  * 커널 드라이버 레이어에서 AI 에이전트 3개의 추론 결과물에 대해 $N=3$ 투표 수행 후 하드웨어 블록에 선택적 명령 하달
  * (본 실효 예시는 경량 로컬형 AI를 비롯하여 클라우드, 엣지, 데이터센터 등 일반 AI 시스템 전반에 동일하게 확장 적용됩니다.)

---

## 7. 결론: 표준 규격을 통한 아키텍처 수렴

시스템의 생존성과 데이터 보안이 최우선되는 환경에서는 결국 **모듈러 조립형 구조로 수렴**됩니다.

핵심은 특정 블록의 제조사가 누구인가가 아니라, **"블록과 블록을 이어 붙이는 다리(Interconnect)와 연결 규칙을 누가 정의하는가"**입니다. 이 연결 규칙과 안전 제어 규격을 오픈 표준으로 선점함으로써, 어떤 연산 블록이 결합되더라도 무중단 생존성을 보장할 수 있습니다.

---

## 8. 실리 보호 및 사후 청구권

* **방어적 공개 및 통상실시권 보장**: 본 아키텍처 규격에 명시된 구성 요소 및 모듈 간 연결 제어 절차는 선행기술(Prior Art)로서 공개됩니다. 본 규격을 준수하는 구현체에 대해서는 누구나 무상으로 통상실시권을 가집니다.
* **특허 독점화에 대한 방어 조치**: 본 규격의 일부 또는 전체를 출원하여 사유화를 시도하는 제3자의 특허에 대해서는 본 문서의 공개 시점을 근거로 무효화 자료로 원용될 수 있으며, 무단 점유 시 사후 사용료 청구 및 권리 침해 금지 청구의 대상이 될 수 있습니다.

---

## 9. 출처 및 권리 명시

* **본 문서의 원안 권리자**: deundeuni (`soma-moa`)
* **공식 원본 도메인**: `somamoa.ai.kr`
* **공개 저장소 원본**: GitHub - `ARCHITECTURE_STRATEGY.md` (커밋 해시 및 일자 타임스탬프 기준)
* **라이선스**: CC BY 4.0 (`https://creativecommons.org/licenses/by/4.0/`) — 출처 표시 시 누구나 자유 이용 및 통상실시권 보장
* **기술적 기반 표준**: UCIe / CXL / TL-UL 등 모듈러 Interconnect 오픈 표준을 참조한 생존형 확장 규격
* **원본 우선 원칙**: 한국어 원문이 기준 원본이며, 번역본은 참고용입니다.
* **방어적 공개 증빙**: 본 문서는 GitHub 공개 커밋 시점과 `somamoa.ai.kr` 게시 시점을 선행기술(Prior Art) 공개 시점으로 합니다.
